# Server configuration
PORT=5000

# Database connection
# Example local MongoDB connection URI:
MONGO_URI=mongodb://localhost:27017/recipebook

# Production MongoDB connection example (replace details):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/recipebook?retryWrites=true&w=majority
